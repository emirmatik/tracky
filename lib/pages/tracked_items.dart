import 'package:flutter/material.dart';

class TrackedItemsPage extends StatelessWidget {
  const TrackedItemsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tracked Items'),
    );
  }
}
